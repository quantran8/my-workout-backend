/**
 * POST /program/generate dùng profile hiện hành của user — không cần body.
 * (Placeholder DTO để giữ chỗ nếu sau này cần tùy chọn gen, vd số tuần / phase override.)
 */
export class GenerateProgramDto {}
